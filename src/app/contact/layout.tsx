export const metadata = {
  title: "Contact Us | Kokalachi",
  description: "Get in touch with Kokalachi. Ask questions, plan a group trip, or just say hello.",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
